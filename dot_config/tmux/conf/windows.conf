# Default mode: indexed (Main/Side/Extra labeled, extras show index only).
# Switch modes live: prefix + M-i (indexed) / prefix + M-l (labeled)
# Mode config lives in windows_indexed.conf and windows_labeled.conf.
# Persists last mode to #{@tmux_config_dir}/window_mode on switch.
set -wg automatic-rename on
if-shell 'test "$(cat #{@tmux_config_dir}/window_mode 2>/dev/null)" = "labeled"' \
  'source-file -F "#{d:current_file}/windows_labeled.conf"' \
  'source-file -F "#{d:current_file}/windows_indexed.conf"'

set -g window-status-style "fg=#{@thm_rosewater}"
set -g window-status-last-style "fg=#{@thm_peach}"
set -g window-status-activity-style "bg=#{@thm_red},fg=#{@thm_bg}"
set -g window-status-bell-style "bg=#{@thm_red},fg=#{@thm_bg},bold"
set -gF window-status-separator "#[fg=#{@thm_overlay_0}]│"
set -g window-status-current-style "bg=#{@thm_peach},fg=#{@thm_bg},bold"